const { getFilenameNormalized } = require('./getFilenameNorm');
const { getFormatDate } = require('./formatDate');
const { getPageDimension } = require('./formatPageDimension');
const { padDigits } = require('./padDigits');

module.exports = {
	getFilenameNormalized,
	getFormatDate,
	getPageDimension,
	padDigits,
};
